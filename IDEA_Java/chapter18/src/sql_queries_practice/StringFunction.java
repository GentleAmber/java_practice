package sql_queries_practice;

public class StringFunction {
    /*
    -- practice: show a list of the names of all the employees with the last letter being uppercase

SELECT CONCAT( LEFT(ename, LENGTH(ename) - 1), UCASE(RIGHT(ename,1)))
FROM emp


     */
}
